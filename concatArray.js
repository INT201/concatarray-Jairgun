const { template } = require('@babel/core')

function concatArray(array1, array2) {
  //code here
  if ((concatArray == null) || (concatArray == undefined)){return undefined}
  else return concatArray
}
module.exports = concatArray
